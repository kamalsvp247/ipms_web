<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- Human-readable: date, level, message (no logger/thread noise) -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="PORTAL" class="com.ivac.booking.networking.PortalLogAppender"/>

    <logger name="com.ivac.booking" level="INFO">
        <appender-ref ref="PORTAL"/>
    </logger>
    <logger name="okhttp3" level="WARN"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
